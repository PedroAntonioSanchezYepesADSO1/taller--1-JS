addEventListener(`DOMContentLoaded`,()=>{
    let lado1=Number(prompt(`ingrese el numero:`)) 
    let lado2=Number(prompt(`ingrese el numero:`))
    let multi=lado1*lado2 

    console.log(`el area del trianguloes:${multi}`);
})