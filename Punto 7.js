addEventListener(`DOMContentLoaded`,()=>{

    //     # DEFINIR VARIABLES NUEMRIAS
    // # DATOS DE ENTRADA
    // ## OPERACIONES BASICAS: * / **
    let div=Number (4/3)
    // ${match.PI}
    let pi=Number (3.14)
    let radio=Number (3)
    // caculo div*pi*radio)**3)
    let res=((div*pi*radio)**3)
    // #salida o entrada de resultado
    console.log(`el volumen de un esfera es:${res}`);
    })